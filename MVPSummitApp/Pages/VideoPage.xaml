<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:pagefactory="clr-namespace:Xamvvm;assembly=Xamvvm.Forms" x:Class="MVPSummitApp.VideoPage" Title="视频" Icon="video.png">
	<ContentPage.Content>

	     <ListView x:Name="VideoListView" RowHeight="120"  HasUnevenRows="false" ItemsSource="{Binding VideoItems}" ItemSelected="Handle_ItemSelected" > 

			<ListView.ItemTemplate>
					  <DataTemplate>
					   <ViewCell  >
						 <ContentView Padding="4" >	
							<StackLayout Orientation="Horizontal">
							   <Image Source="{Binding VideoImg}" WidthRequest="160" HeightRequest="90"  />
							   <StackLayout Padding="4">
									<Label Text="{Binding VideoName}" FontSize="18" Margin="2,10" />
									<StackLayout Orientation="Horizontal" Padding="2">

									   <Label Text="{Binding VideoVendor}" FontSize="14"/><Label Text="{Binding VideoDate}" FontSize="14"/>
									</StackLayout>
                               </StackLayout>	
							</StackLayout>
						 </ContentView>
					   </ViewCell>
					  </DataTemplate>
				</ListView.ItemTemplate>
		</ListView>	
	</ContentPage.Content>
</ContentPage>
